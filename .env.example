# OpenAI API Key (for GPT-4 and DALL-E)
OPENAI_API_KEY=sk-your-openai-api-key-here

# WordPress Configuration
WP_URL=https://your-wordpress-site.com
WP_USERNAME=your-username
WP_APP_PASSWORD=your-application-password

# RSS Feeds (comma-separated URLs)
RSS_FEEDS=https://fetchrss.com/feed/1vcaujD2G3c21vcatkBil4m3.rss

# Optional: Polling interval in minutes (default: 30)
POLL_INTERVAL_MINUTES=30

# Optional: Image storage directory (default: ./images)
IMAGE_DIR=./images

# Optional: Database file path (default: ./processed.db)
DATABASE_PATH=./processed.db
